
Create the directory in desktop:

Mkdir passwordcrack

Cd passwordcrack

open the commandline prompt and types the below command
1. Nano hasser.py

#!/user/bin/python
Import hashlib
Hashvalue=input(“Enter a string to hash”)
Hashobj1=hashlib.md5()
Hashobj1.update(hashvalue.encode())
Print(hashobj1.hexdigest())
Hashobj2=hashlib.shal()
Hashobj2.update(hashvalue.encode())
Print(Hashobj2.hexdigest())

change the file ownership from commandline.

Command prompt=chmode+x hasher.py
running the python from commmandline prompt
Python3 hasher.py


Create the directory in desktop:

Mkdir passwordcrack

Cd passwordcrack

open the commandline prompt and types the below command
1. Nano hasser.py

#!/user/bin/python
Import hashlib
Hashvalue=input(“Enter a string to hash”)
Hashobj1=hashlib.md5()
Hashobj1.update(hashvalue.encode())
Print(hashobj1.hexdigest())
Hashobj2=hashlib.shal()
Hashobj2.update(hashvalue.encode())
Print(Hashobj2.hexdigest())

change the file ownership from commandline.

Command prompt=chmode+x hasher.py
running the python from commmandline prompt
Python3 hasher.py


Password_Matching using python for hacker

Import urllib.request import urlopen
Import hashlib
Shalhash=input(“[*] Enter the hash value :”)
Passlist=str("https://github.com/danielmiessler/SecLists/blob/master/Passwords/Common-Credentials/10-million-password-list-top-1000.txt”).read(). ’utf-8’)
For password in passlist.split(‘\n’):
hashguess=Hashlib.shal(bytes(password, ‘utf-8’)).hexdigest()
if hashguess==shalhash:
print(“[+] The password is : ” +str(password))
quite()
else:
print(“[.] password guess” + str(password) + “Doesn’t matching ”)


